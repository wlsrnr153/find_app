# 수정 버튼 문제 디버깅 가이드 🔍

## 🚨 문제 상황
기존 계정으로 로그인했을 때 자신이 작성한 물품의 수정 버튼이 나타나지 않는 문제

---

## 🔍 디버깅 방법

### 1단계: 페이지 새로고침
1. 브라우저에서 **F5** 키를 눌러 페이지 새로고침
2. 목록 탭으로 이동

### 2단계: 화면에서 디버그 정보 확인

각 물품 카드 하단에 다음과 같은 정보가 표시됩니다:

```
작성자ID: abc123 | 내ID: abc123 | 역할: user | 수정가능: ✅
```

**체크 포인트:**
- ✅ `작성자ID`와 `내ID`가 **같으면** → 수정 버튼이 보여야 함
- ❌ `작성자ID`가 `없음`이면 → 모든 사용자가 수정 가능 (기존 물품)
- ❌ `작성자ID`와 `내ID`가 **다르면** → 수정 버튼 안 보임 (정상)

### 3단계: 브라우저 콘솔 확인 (F12)

콘솔에 다음과 같은 로그가 출력됩니다:

```
[권한체크] 물품: "책상" | 물품userId: "abc123" | 현재사용자: "abc123" | 역할: "user" | 작성자: true | 소유자없음: false | 수정가능: true
```

**체크 포인트:**
- `작성자: true` → 본인이 작성한 물품
- `소유자없음: true` → 기존 물품 (모든 사용자 수정 가능)
- `수정가능: true` → 수정 버튼이 보여야 함
- `수정가능: false` → 수정 버튼 안 보임

---

## 📋 예상 시나리오

### 시나리오 1: 본인이 작성한 물품 ✅
```
작성자ID: Abc123XYZ | 내ID: Abc123XYZ | 역할: user | 수정가능: ✅
```
**결과**: 수정 버튼 표시됨 (정상)

### 시나리오 2: 기존 물품 (userId 없음) ✅
```
작성자ID: 없음 | 내ID: Abc123XYZ | 역할: user | 수정가능: ✅
```
**결과**: 수정 버튼 표시됨 (하위 호환성)

### 시나리오 3: 다른 사용자의 물품 ❌
```
작성자ID: Xyz789ABC | 내ID: Abc123XYZ | 역할: user | 수정가능: ❌
```
**결과**: "다른 사용자의 물품" 텍스트 표시됨 (정상)

### 시나리오 4: 관리자 ✅
```
작성자ID: Xyz789ABC | 내ID: Abc123XYZ | 역할: admin | 수정가능: ✅
```
**결과**: 수정 버튼 표시됨 (관리자 권한)

---

## 🐛 문제 발생 시 확인 사항

### 문제 1: 모든 물품에서 수정 버튼이 안 보임

**확인:**
```
내ID: 없음
```

**원인**: `currentUser`가 undefined
**해결**: 페이지 새로고침 또는 다시 로그인

---

### 문제 2: 본인 물품인데 수정 버튼이 안 보임

**확인:**
```
작성자ID: Abc123 | 내ID: Xyz789 | 수정가능: ❌
```

**원인**: 
1. 다른 계정으로 작성한 물품
2. 또는 userId가 잘못 저장됨

**해결**:
- 어떤 계정으로 작성했는지 확인
- 새로 물품을 등록해서 테스트

---

### 문제 3: 역할이 잘못 표시됨

**확인:**
```
역할: undefined 또는 null
```

**원인**: 사용자 역할 캐시 문제

**해결**:
1. 브라우저 콘솔에서 실행:
```javascript
localStorage.removeItem('userRole_' + firebase.auth().currentUser.uid);
localStorage.removeItem('userRole_' + firebase.auth().currentUser.uid + '_time');
```
2. 페이지 새로고침

---

## 🧪 테스트 절차

### 1. 새 물품 등록 테스트
1. 입력 탭에서 새 물품 등록
2. 목록 탭으로 이동
3. 방금 등록한 물품 확인

**예상 결과:**
```
작성자ID: [내ID] | 내ID: [내ID] | 수정가능: ✅
✏️ 수정 버튼 표시됨
```

### 2. 다른 계정으로 테스트
1. 로그아웃
2. 다른 계정으로 로그인
3. 목록 탭에서 이전 계정의 물품 확인

**예상 결과:**
```
작성자ID: [이전ID] | 내ID: [현재ID] | 수정가능: ❌
"다른 사용자의 물품" 텍스트 표시됨
```

### 3. 관리자 계정으로 테스트
1. 관리자 계정으로 로그인
2. 모든 물품 확인

**예상 결과:**
```
역할: admin | 수정가능: ✅
모든 물품에 ✏️ 수정 버튼 표시됨
```

---

## 📸 스크린샷 가이드

문제가 계속되면 다음 정보를 캡처해서 공유해 주세요:

### 1. 화면 캡처
- 물품 카드의 디버그 정보 (하단 작은 글씨)
- 수정 버튼 또는 "다른 사용자의 물품" 텍스트

### 2. 콘솔 로그 복사
```
[권한체크] 물품: "..." | 물품userId: "..." | 현재사용자: "..." | ...
```
이 로그를 복사해서 공유

---

## 🔧 강제 권한 체크 (개발자용)

브라우저 콘솔에서 실행:

```javascript
// 현재 로그인한 사용자 정보
console.log('현재 사용자:', firebase.auth().currentUser);
console.log('사용자 ID:', firebase.auth().currentUser?.uid);
console.log('사용자 역할:', currentUserRole);

// 첫 번째 물품의 권한 정보
console.log('첫 번째 물품:', items[0]);
console.log('물품 userId:', items[0]?.userId);
console.log('일치 여부:', items[0]?.userId === firebase.auth().currentUser?.uid);
```

---

## ✅ 정상 작동 확인

다음과 같이 표시되면 정상:

### 본인이 작성한 물품:
```
[물품 카드]
작성자ID: Abc123XYZ | 내ID: Abc123XYZ | 역할: user | 수정가능: ✅
✏️ 수정 [버튼]
```

### 다른 사용자의 물품:
```
[물품 카드]
작성자ID: Xyz789ABC | 내ID: Abc123XYZ | 역할: user | 수정가능: ❌
다른 사용자의 물품
```

### 기존 물품 (userId 없음):
```
[물품 카드]
작성자ID: 없음 | 내ID: Abc123XYZ | 역할: user | 수정가능: ✅
✏️ 수정 [버튼]
```

---

## 🚀 다음 단계

1. **페이지 새로고침** (F5)
2. **브라우저 콘솔 열기** (F12)
3. **목록 탭으로 이동**
4. **디버그 정보 확인**
5. 문제가 계속되면 위 정보를 공유

---

**참고**: 
- 디버그 정보는 임시로 표시되며, 문제 해결 후 제거될 예정입니다.
- 모든 물품에 대한 권한 체크 로그가 콘솔에 출력됩니다.

**작성일**: 2025-11-10

