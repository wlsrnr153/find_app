# 🔧 인증 오류 해결 가이드

## ❌ 오류: auth/invalid-login-credentials

### 오류 의미
"로그인 자격 증명이 잘못되었습니다"

이 오류는 다음 중 하나를 의미합니다:
1. 이메일이 틀림
2. 비밀번호가 틀림
3. 계정이 존재하지 않음

---

## 🔍 원인 분석 및 해결 방법

### 1단계: Firebase Console에서 사용자 확인

1. **Firebase Console 접속**
   ```
   https://console.firebase.google.com/project/hyundai-e653c/authentication/users
   ```

2. **사용자 목록 확인**
   - 로그인하려는 이메일이 목록에 있는지 확인
   - 없다면 → **회원가입 필요**
   - 있다면 → **비밀번호 확인 필요**

### 예시 스크린샷 (Firebase Console)
```
Authentication > Users

| 이메일              | UID          | 생성 날짜      | 로그인    |
|---------------------|-------------|---------------|----------|
| user1@test.com      | abc123...   | 2025-11-10    | 1일 전   |
| user2@test.com      | def456...   | 2025-11-10    | 5분 전   |
```

---

## ✅ 해결 방법

### 방법 1: 새 계정 생성 (다른 컴퓨터에서)

다른 컴퓨터에서도 **새로 회원가입** 해야 합니다:

1. 물품조사 시스템 접속
2. "회원가입" 탭 클릭
3. 새 이메일 + 비밀번호 입력
4. 회원가입 완료

**중요:** 
- 각 사람마다 **다른 이메일** 필요
- 또는 같은 이메일/비밀번호를 **공유**

---

### 방법 2: 계정 정보 공유

**팀원들이 같은 계정 사용하려면:**

```
관리자 PC에서 생성한 계정:
📧 이메일: survey@hjs4393.com
🔑 비밀번호: hjs2025!@#

→ 이 정보를 팀원들에게 공유
→ 팀원들이 "로그인" 탭에서 이 정보로 로그인
```

---

### 방법 3: 비밀번호 재설정

계정은 있는데 비밀번호를 모르는 경우:

1. 로그인 페이지에서
2. "비밀번호를 잊으셨나요?" 클릭
3. 이메일 입력
4. Firebase가 비밀번호 재설정 이메일 발송
5. 이메일에서 링크 클릭하여 새 비밀번호 설정

---

## 🧪 테스트 방법

### 단계별 테스트:

#### 1. 로컬 PC에서 테스트
```
1. https://hyundai-e653c.web.app 접속
2. 회원가입: test1@test.com / test123456
3. 로그인 확인 ✅
```

#### 2. 다른 PC에서 같은 계정으로 테스트
```
1. https://hyundai-e653c.web.app 접속
2. 로그인: test1@test.com / test123456
3. 로그인 확인 ✅
```

#### 3. 다른 PC에서 새 계정으로 테스트
```
1. https://hyundai-e653c.web.app 접속
2. 회원가입: test2@test.com / test123456
3. 로그인 확인 ✅
```

---

## 🔍 디버깅 방법

### 브라우저 콘솔에서 확인

1. **F12** 키 (개발자 도구)
2. **Console** 탭
3. 로그인 시도
4. 오류 메시지 확인

**예상 메시지:**
```javascript
// 계정이 없는 경우
Firebase: Error (auth/user-not-found)

// 비밀번호가 틀린 경우
Firebase: Error (auth/wrong-password)

// 이메일 형식이 틀린 경우
Firebase: Error (auth/invalid-email)
```

---

## 📊 상황별 해결 가이드

### 상황 1: "처음 사용하는 사람"
```
문제: 다른 PC에서 로그인 안됨
원인: 계정이 없음
해결: 회원가입 필요 ✅
```

### 상황 2: "이미 계정 만든 사람"
```
문제: 다른 PC에서도 로그인 안됨
원인: 이메일/비밀번호 틀림
해결: 
  1. 이메일 주소 다시 확인
  2. 비밀번호 재입력 (대소문자 구분)
  3. 비밀번호 재설정
```

### 상황 3: "팀 계정 공유"
```
문제: 여러 명이 하나의 계정 사용
원인: 계정 정보가 공유되지 않음
해결:
  1. 관리자가 계정 생성
  2. 이메일/비밀번호를 팀원들에게 전달
  3. 모두 같은 계정으로 로그인 ✅
```

---

## 🚫 Firebase가 차단하는 경우

### Firebase가 실제로 차단하는 경우:

1. **너무 많은 로그인 시도**
   ```
   오류: auth/too-many-requests
   해결: 1시간 대기 또는 비밀번호 재설정
   ```

2. **계정 비활성화**
   ```
   오류: auth/user-disabled
   해결: Firebase Console에서 계정 활성화
   ```

3. **이메일 인증 필요 (설정한 경우)**
   ```
   오류: auth/unverified-email
   해결: 이메일에서 인증 링크 클릭
   ```

**현재 오류 `auth/invalid-login-credentials`는 위 경우가 아닙니다!**

---

## ✅ 권장 해결책

### 개인 계정 방식 (권장)
```
각 팀원이 자신의 이메일로 회원가입:
- user1@hjs.com
- user2@hjs.com
- user3@hjs.com

장점:
✅ 누가 입력했는지 명확함
✅ 보안 강화
✅ 개별 권한 관리 가능
```

### 공유 계정 방식
```
팀 전체가 하나의 계정 공유:
- survey@hjs4393.com / hjs2025!

장점:
✅ 관리 간편
✅ 계정 하나만 생성

단점:
⚠️ 누가 입력했는지 구분 어려움
⚠️ 비밀번호 변경 시 모두에게 알려야 함
```

---

## 🔧 Firebase Console에서 직접 계정 생성

관리자가 Firebase Console에서 직접 계정 생성 가능:

1. **Firebase Console 접속**
   ```
   https://console.firebase.google.com/project/hyundai-e653c/authentication/users
   ```

2. **"사용자 추가" 클릭**

3. **정보 입력**
   ```
   이메일: worker1@hjs.com
   비밀번호: Hjs2025!@#
   ```

4. **"사용자 추가" 클릭**

5. **팀원에게 로그인 정보 전달**

---

## 📱 모바일 vs PC

### 중요: 계정은 기기와 무관합니다!

```
✅ 같은 계정으로:
   - PC에서 로그인
   - 스마트폰에서 로그인
   - 태블릿에서 로그인
   모두 가능!

❌ 다른 기기 = 다른 계정 필요 (X)
✅ 같은 계정 = 모든 기기에서 사용 (O)
```

---

## 🎯 체크리스트

문제 해결을 위한 체크리스트:

### 확인 사항
- [ ] Firebase Console에서 사용자 목록 확인
- [ ] 로그인하려는 이메일이 목록에 있는지 확인
- [ ] 이메일 철자 확인 (대소문자, 공백)
- [ ] 비밀번호 확인 (대소문자 구분)
- [ ] 브라우저 콘솔에서 정확한 오류 메시지 확인

### 해결 방법
- [ ] 계정이 없으면 → 회원가입
- [ ] 계정이 있으면 → 비밀번호 재확인 또는 재설정
- [ ] 팀 계정 사용 → 로그인 정보 공유
- [ ] 개인 계정 사용 → 각자 회원가입

---

## 💡 추가 팁

### 1. 비밀번호 요구사항
```
Firebase 기본 요구사항:
- 최소 6자 이상
- 특수문자, 대문자 등 제한 없음

권장:
- 8자 이상
- 영문 + 숫자 조합
예: hjs2025, survey123
```

### 2. 이메일 형식
```
✅ 올바른 형식:
- user@example.com
- test123@gmail.com
- survey@hjs4393.com

❌ 잘못된 형식:
- user@
- @example.com
- user example.com (@ 없음)
```

### 3. 테스트 계정 만들기
```
개발/테스트용:
이메일: test@test.com
비밀번호: test123456

→ 팀원 모두에게 공유하여 테스트
```

---

## 📞 추가 지원이 필요한 경우

1. **Firebase Console 스크린샷 확인**
   - Authentication > Users 화면 캡처
   - 사용자 목록 확인

2. **브라우저 콘솔 오류 확인**
   - F12 → Console 탭
   - 오류 메시지 복사

3. **로그인 시도 정보**
   - 사용한 이메일 주소
   - 오류 발생 시점
   - 어떤 PC/브라우저에서 발생하는지

---

**작성일**: 2025-11-10  
**프로젝트**: hyundai-e653c  
**오류**: auth/invalid-login-credentials
